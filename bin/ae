#!/usr/bin/env zsh

set -euo pipefail

AE_REPO="${AE_REPO:-$(cd "$(dirname "${0:A}")/.." && pwd)}"
PYTHON="$AE_REPO/.venv/bin/python"
export AE_REPO
export PYTHONPATH="$AE_REPO/src${PYTHONPATH:+:$PYTHONPATH}"

if [[ ! -x "$PYTHON" ]]; then
  PYTHON="python3"
fi

exec "$PYTHON" -m ae.cli "$@"
